spring.datasource.url=jdbc:derby:memory:test;create=true
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.open-in-view=true

#Die Anzahl der IDs, die für die automatische Vergabe reserviert werden (Default ist 100).
#Damit keine IDs übersprungen werden, wenn das System nicht korrekt herunterfährt, wird sie auf 1 gesetzt.
#derby.language.sequence.preallocator=1

org.salex.raspberry.camera.script=/usr/bin/raspistill

org.salex.raspberry.workshop.blog.picture.defaultWidth=800
org.salex.raspberry.workshop.blog.picture.defaultHeight=600

